<UserControl x:Class="AbsoluteMouseToJoystick.Views.AxisZoneSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AbsoluteMouseToJoystick.Views"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <TextBox Text="{Binding NegativeDeadZone}" Margin="3"/>
        <TextBox Text="{Binding NegativeZone}" Margin="3" Grid.Column="1"/>
        <TextBox Text="{Binding NeutralDeadZone}" Margin="3" Grid.Column="2"/>
        <TextBox Text="{Binding PositiveZone}" Margin="3" Grid.Column="3"/>
        <TextBox Text="{Binding PositiveDeadZone}" Margin="3" Grid.Column="4"/>
    </Grid>
</UserControl>
